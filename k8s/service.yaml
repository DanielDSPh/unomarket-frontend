apiVersion: v1
kind: Service
metadata:
  name: unomarket-frontend-svc
  namespace: demo
spec:
  type: NodePort
  selector:
    app: unomarket-front
  ports:
    - port: 80
      targetPort: 80
      nodePort: 31080